{
  "name": "techleadership-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site for Andrew Swerdlow — Tech Leadership (book, speaking, advising, coaching).",
  "scripts": {
    "dev": "python3 -m http.server 8765 --directory public",
    "qa": "python3 qa.py",
    "qa:mobile": "node scripts/mobile-qa.mjs",
    "qa:mobile:live": "node scripts/mobile-qa.mjs https://techleadership.ai",
    "deploy": "sh -c 'set -a; [ -f .cloudflare.env ] && . ./.cloudflare.env; set +a; npx wrangler pages deploy public --project-name=techleadership --branch=main'"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "playwright-core": "^1.60.0"
  }
}
